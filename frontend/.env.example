# URL of the Express backend (internal service URL on Zeabur, or localhost for dev)
# Used by: next.config.ts rewrites + Server Components fetching from BACKEND_URL
BACKEND_URL=http://localhost:3001

# Public URL of this Next.js app (used for absolute URLs if needed)
NEXT_PUBLIC_APP_URL=http://localhost:3000
