{
  "name": "aeo-monorepo",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build:frontend": "npm run build --workspace frontend",
    "build:backend": "npm run db:generate --workspace backend",
    "start:frontend": "npm run start --workspace frontend",
    "start:backend": "npm run start --workspace backend"
  }
}
